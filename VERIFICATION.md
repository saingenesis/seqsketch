# 首版验证记录

日期：2026-09-08。测试数据均为程序生成或测试用鼠标输入，不是研究采集样本。

## 作品管理、导出位置与双模式更新

- TypeScript 类型检查、正式构建和全部 25 项功能测试通过。新增覆盖作品删除、两种模式分组、编辑和删除后的顺序、旧工程读取，以及通过本地服务实际写入和更新文件。
- 在便携版的 5317 页面验证：示例在曲线模式为 175 条，在落笔模式为 153 条；第一笔包含 4 段，能选择其中第 2 段。落笔顺序预览第 1 步同时显示这 4 段。
- 作品库中取消删除保留作品；确认删除专用测试作品后总数从 3 降为 2，刷新后该作品仍未出现。
- 在导出窗口填写测试文件夹，实际生成两种模式各一份训练 JSON 和 SVG，以及一份完整工程。读取磁盘文件确认：训练 JSON 为 1.1，分别含 175 / 153 个有序 `lines`，落笔模式第一条包含 4 段；SVG 分别含 175 / 153 个 path；完整工程为 1.1，并保留落笔模式和全部 175 段。
- 刷新后导出文件夹和所选记录模式均保留。导出成功提示包含实际文件路径，未发现浏览器错误或警告。
- 检查桌面 1280 × 720 和小屏幕 390 × 844 的导出窗口，输入框、三种导出按钮与完成按钮均完整可用。
- 未进行真实数位笔和多人采集实测，也未完成浏览器选择工程文件的导入往返验证。以下为历史验证记录，其中旧下载流程的限制已由本次实际文件导出验证补充。

## 便携版空白问题修复后的补充验证

Paper.js 完整版的 PaperScript 初始化与便携服务的页面安全策略冲突，现已通过 Vite 配置改用同版本核心构建。已重新通过类型检查、正式构建和全部 19 项核心测试。

在 http://127.0.0.1:5317/ 实际验证：页面及 175 段曲线的人像示例显示正常；侧栏坐标编辑与撤销恢复原值；删除与撤销恢复曲线数量；刷新恢复保存的作品；顺序播放从 0 到达 175。修复后未出现新的浏览器错误或警告。本次未操作真实数位笔，也未重新验证文件导入导出流程。以下为首版历史记录。

## 已通过

- TypeScript 类型检查。
- 19 项自动测试：曲线拟合端点与方向、落笔分组、删除后的创建顺序、编辑身份保留、归一化、SVG 输出与转义、工程解析校验、示例标记、撤销恢复与分支。
- 正式网站构建，已附带第三方许可声明。
- npm 依赖审计，0 项已知漏洞。
- Chrome 中实际画出两次落笔，对应 2 段曲线、2 次落笔；选择曲线后显示其四个控制点坐标。
- 合成人像示例显示 175 段曲线、153 次落笔，画布非空且完整可见。
- 顺序播放实际呈现部分几何，观察到 156 / 175 的预览状态。
- 刷新页面后从本机保存恢复同一示例及其 175 段曲线。
- 桌面约 1463 × 690 和小屏幕 390 × 844 的截图检查；画布、侧栏和主要工具栏布局正常。
- 导出对话框的训练数据按钮触发，并显示已生成训练数据。

## 尚未完成

- 浏览器中的节点拖动、删除与撤销完整操作流程，自动审批连续超时，未取得足够的完成证据。相应底层数据操作已有自动测试。
- 浏览器实际选择本地工程文件的流程：文件选择器返回 `Not allowed`。Chrome 的 ChatGPT 浏览器扩展未获文件 URL 访问权限，未修改该设置。工程解析已有自动测试。
- 下载文件的浏览器事件等待超时，未通过该事件核实磁盘落盘位置。导出函数的数据及 SVG 格式已有自动测试，但仍需手动下载后重新打开工程验证完整往返流程。
- 真实数位笔的延迟、平滑度、侧键、橡皮端、手掌接触，以及多人课堂采集。
- 大量复杂作品或单幅上万段曲线的性能压力测试。

建议先用实际采集设备完成少量试画、下载工程、重新打开、删除撤销及顺序导出的检查，再开始正式批量采集。
